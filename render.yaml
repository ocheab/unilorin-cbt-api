services:
  - type: web
    name: unilorin-cbt-api
    runtime: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: DB_PATH
        value: /var/data/cbt_admin.sqlite
      - key: ADMIN_API_KEY
        sync: false
